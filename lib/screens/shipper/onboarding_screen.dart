// Shipper onboarding removed
// If shipper-specific onboarding is required in the future, implement a
// focused form/screen and register its route explicitly. Keeping this stub
// prevents accidental import failures while removing the UI from the app.
